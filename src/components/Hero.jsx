import styled from "styled-components";

const Section = styled.div`
  height: 100vh;
  background-color: rebeccapurple;
`;

const Hero = () => {
  return (
   <Section>This is the Hero component</Section>
  )
}

export default Hero;