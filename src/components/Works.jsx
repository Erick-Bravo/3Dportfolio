import styled from "styled-components";

const Section = styled.div`
  height: 100vh;
  scroll-snap-align: center;
`;

const Works = () => {
  return (
   <Section>This is the Works component</Section>
  )
}

export default Works;